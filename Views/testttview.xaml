<UserControl x:Class="InventoryApplication.Views.testttview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryApplication.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"            
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel>
            <StackPanel Orientation='Horizontal'>
                <DatePicker x:Name="mydate" Width="200" Style="{DynamicResource MaterialDesignFloatingHintDatePicker}"/>
                <materialDesign:TimePicker x:Name="mytime" Width="100" Is24Hours="True" Style="{DynamicResource MaterialDesignFloatingHintTimePicker}"/>
            </StackPanel>
            <Button Style="{DynamicResource MaterialDesignFloatingActionButton}" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
